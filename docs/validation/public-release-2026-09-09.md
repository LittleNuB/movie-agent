# 首次公开源码预检 · 2026-09-09

范围：将当前 Movie Agent 实现整理为 `v0.1.0-preview.1` 源码预览包，同步默认分支并公开仓库。授权与数据范围见 [ADR 0009](../adr/0009-public-source-release.md)。本页记录发行前检查；具体发行提交、资产与校验和以 [GitHub Release](https://github.com/LittleNuB/movie-agent/releases/tag/v0.1.0-preview.1) 为准。

## 已完成检查

| 检查 | 结果与边界 |
|---|---|
| 原有可达 Git 历史 | 检查全部 30 条原始提交、336 个不同文件内容对象；未发现受版本控制的运行数据库、依赖目录、密钥文件或生成影片 |
| 凭据扫描 | 使用 Gitleaks 8.30.1 扫描全部原有可达提交，未发现泄漏；另检查私钥、JWT、个人绝对路径、个人密钥文件名和签名 URL 模式。扫描是预检，不是绝对无泄漏保证 |
| 邮箱与提交作者 | 文件内容唯一邮箱形式命中为 `prototype/verify_settings.py` 的 example.com 用户密码 URL 拒绝测试；原提交作者信息经用户明确选择保留，未改写历史 |
| 文档图片 | 人工查看 7 张受版本控制 PNG，包括主题、Logo 探索和原型截图；未见真实凭据。原型／示意图不冒充当前真实影片 |
| 许可和来源 | 新增项目 MIT 许可，保留 ViMax 固定版本、改写说明和原 MIT 声明；依赖与 FFmpeg 二进制不随源码包发行 |
| Python 检查 | Ruff 通过；58 项 pytest 通过，无跳过。出现一条 Starlette / AnyIO 的弃用警告，不影响本轮通过结果 |
| 前端文本检查 | Node 测试 4 项通过；没有据此宣称完整浏览器交互或电影质量验收通过 |
| 锁定依赖 | 更新项目许可和文档元数据后，`uv lock --check --offline` 通过；运行依赖及锁定版本未改变 |
| 打包与链接 | 从暂存树生成的 ZIP 含 131 个文件，与 Git 清单一致；349 个文档／页面本地路径检查通过，无运行数据或依赖环境。解压源码的 Gitleaks 扫描也未发现泄漏 |
| 解压初始化 | 在独立解压目录运行 `setup.ps1 -SkipMediaTools`，创建新虚拟环境并安装 114 个锁定包成功；本次复用依赖下载缓存，没有重新验证 FFmpeg 下载或全新 Windows 机器安装 |
| 空配置启动 | 从解压源码导入真实应用，启用运行时的启动／关闭检查通过；配置和项目初始为空，5 个静态资源可读取，共创／托管可建项目，已取消模式及跨站写入被拒绝。没有发送模型消息或触发付费调用 |
| GitHub 既有内容 | 发布前远程 Actions 运行、Issue／PR、Release 和提交评论均为 0；没有需另行公开审查的既有附件或运行日志 |

源码包仅由明确暂存并提交的 Git 文件生成，不使用整个工作目录压缩。最终提交后重新扫描历史，发行时核对远程提交、公开可见性、README 和下载校验和；没有发布个人数据库、历史生成素材、Key 或扫描原始报告。源码解压检查属于接口及运行检查，不替代本轮未重新执行的完整浏览器体验。

本轮没有付费模型生成，没有更改用户影片项目，也没有补做整片人评。README 保留独立配乐、托管判断、参考遗漏、故事人评及完整验证矩阵未通过的事实。开源不改变这些结论。
