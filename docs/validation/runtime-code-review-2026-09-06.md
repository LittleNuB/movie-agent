# 运行 MVP · 代码复核

日期：2026-09-06。基准 `6f15bed21a6ca52d3f0097fb45ae5f3136c518ba`，分支 `feat/movie-runtime`。首次检查提交 `9ac54f6630a102a68b8242b4e8aab1ccb557d1c0`，随后对工作树修复持续复核。范围是用户授权的真实电影 MVP、[实施规格](../plans/runtime-implementation.md)和 AGENTS 规则；没有把完整影片尚未验收当作已交付。

采用 code-review skill 的独立 Standards 与 Spec 只读复核。审查助手没有读取凭据、调用模型或修改代码。

## Standards

初次指出消息／审核到导演投递间的崩溃窗口、失败吞掉后续输入、审核显示与待审版本混淆，已修复并补充边界测试。

后续指出已有下载结果依然依赖已移除连接、取消／过期状态阻断合法重试、中止后的媒体通知可能被聊天失败消耗，以及旧下载 URL 失效后无法刷新。逐项修复；最终只读复核确认旧 URL 403 → 查询原任务 → 新 URL 下载的实现与测试，不生成新任务。最终指定复核范围无新增问题。

## Spec

发现并修复：未采用的直接制作依据、阶段参数绕过视觉审核、旧成片／试拍混用新剧本、自然语言认可缺少落地工具、迟到专业结果冒用当前版本，以及短音轨淡出时间错误。

进一步修复导演读取旧依据后委派时被重新标为新依据的窗口：委派携带观察到的依据，比较当前采用状态，去重包含依据；专业运行重启仍保留原始依据。审核同时校验候选标记与同类父版本，变化判断包含新增采用项。最终只读复核确认这些修复，未发现新的实质问题。

## 实际验证

- 最终全套 `pytest -q`：37 passed，24.30 秒；一个上游 Starlette/AnyIO 弃用提示。涵盖初始28项及后续规格、型号变更、声音复用等实际问题的回归。
- Ruff：通过；`node --check web/app.js` 与 `web/settings.js`：通过；Git 空白检查通过。
- 本地 Markdown 链接检查：222 条有效；候选文件及本机数据库／日志共164份凭据扫描无匹配，运行数据被忽略。
- 候选提交文件与本机框架／业务 SQLite、日志扫描：未匹配真实配置 Key；运行数据和产物仍排除在 Git 外。
- 真实浏览器、24 个真实 LLM 用例、真实媒体与费用边界见[运行证据](runtime-evidence-2026-09-06.md)。这些结果不替代尚未完成的两个完整制作、修改和人评。

Standards：指定问题已解决，无未处理发现。Spec：指定错误实现已解决；整轮产品验收继续进行。

独立复核后的小范围补充：720P 在生成前拦截更高规格，原高分辨率母版本地派生且不覆盖；Ark 带 error 的已知任务失败回执按终态处理，与查询失败区分，不自动重发被内容审核拒绝的原请求。上述分支已有 Provider／恢复与真实 FFmpeg 测试，未把它们写成整片人评通过。另补充手改范围和标注位置提示，真实语义回归仍待执行。

2026-09-07 增量复核按用户最终规格 H3 768P／Seedance Fast 720P检查，发现旧标准版 pending 任务可能在改用Fast后仍首次提交。已在真正POST前复核当前非秘密binding与入队快照／expected_model；只读复核确认P1闭合，旧外部ID仍按原binding查询。对应测试通过。

另从实际影片比较中发现混合声重复叠加并遗漏独立台词，新增保留完整原剪辑的导出工具和来源重叠检查。37项测试包含该回归；正确720P前后版的前78秒解码音频相同，独立台词与字幕相同。四个真实语义回归已执行，改善及残留歧义见运行记录；未宣称提示修复已使全部语义场景通过。
