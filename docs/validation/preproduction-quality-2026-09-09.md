# 前期制作工具首批验收

日期：2026-09-09。依据：[ADR 0007](../adr/0007-preproduction-and-reference-engineering.md)、[任务规格](../plans/preproduction-quality.md)。固定起点 `587d50b`；文档提交 `48bb46f`，初版工具 `9f3a51f`，审查修复 `d006193`。

此页保留首批工程阶段事实；同日继续实施的真实模型、媒体与有声预演见[后续记录](./preproduction-live-2026-09-09.md)，不以新结果改写以下历史状态。

## 实际范围

本轮先形成决策、实施规格、术语和来源记录，再实现Q1—Q3第一版：结构化制作说明、参考清单、镜头编译／独立提交，以及资产库的来源导航。没有重新生成既有电影、修改用户剧本、采用版本或真实模型配置。

新工具可由AgentScope调用；没有新增固定角色、预算门槛或运行时趣味评分。当前能力是保存、结构／引用校验与实际请求绑定，不是自动逻辑正确或人物一致性的保证。

## 工程验证

| 检查 | 证据与边界 |
|---|---|
| 制作说明与版本 | 同项目剧本身份、实体／事件引用、草稿和采用关系不受保存影响；SQLite重开保留结构 |
| 参考来源 | 跨项目素材、虚构派生关系、缺少真实文件或媒体观察记录时拒绝；候选可保留但不能直接充当已检查标准 |
| 镜头编译 | 输入模式、引用类型、事件身份与不用参考理由校验；保存模型身份时排除Key；编译不提交任务 |
| 实际调用链 | 受控Provider替身核对编码后的文件参考与编译提示到达提交边界，返回素材保留镜头输入身份；没有外部网络生成 |
| 审核与恢复 | 既有审核和停止门禁继续生效；排队时剧本已改变则在外部发送前拒绝；迟到、恢复与历史版本回归保留 |
| 通用入口 | 从编译类型的依据推导身份，省略可选字段不能改用另一份提示词；任务保存与发送前重复核对 |
| 页面 | 本地静态页面配受控快照，所有路由拦截：新类型搜索、文稿阅读、来源跳转与反向引用通过；没有写请求或JavaScript错误 |

初版全量Python回归51项通过；审查修复后针对制作工具、恢复和授权的29项通过，最终全量52项通过。Ruff、JavaScript语法和3项Node结构测试通过。存在一条既有依赖弃用提示，未新增调用失败。149个本地文档链接检查无失效项，所检查的凭据模式未命中，Git空白检查通过。

浏览器使用1600×1000视口，已查看制作说明和编译输入两张截图，文稿、来源按钮与侧栏可读，无页面横向溢出。首次访问4318发现服务未运行，随后全部使用拦截路由；没有为测试启动旧项目的运行时。该检查不证明真实模型使用新流程。

本机证据在忽略版本控制的 `local-data/preproduction-validation-2026-09-09/`，包括 `browser-result.json`、`compiled-input.png`、`production-brief.png` 和明确标注为工程示例的快照。示例素材不属于用户影片，未作为真实生成交付。

## 审查

按code-review skill分别执行Standards和Spec审查。

- Standards：未发现可确认的仓库规则违反或需要单列的代码气味。
- Spec：发现一项P2，通用生成工具引用编译镜头为依据时，省略可选身份可绕过一致性检查。已在 `d006193` 修复并补直接工具／任务提交回归；原审查者复核确认关闭。

两项结论分别记录；它们不是对电影质量的评价。

## 未通过或尚未执行

- 未运行真实LLM来验证导演是否稳定组织新文稿、正确选参考或编译提示是否忠实；工具Schema、受控调用成功不能替代这一层。
- 未新增图片、视频、配音或配乐生成；本轮没有使用新的媒体额度，也没有宣称继承了完整的旧走查额度。
- 尚未实现独立音频的人评证据入口、自动人物多视图派生编排、有声预演和新的连续性观察算法。当前媒体观察的抽帧与无听音限制保持不变。
- 旧自由格式项目不自动补造制作说明；通用原始生成仍可用于旧工作，导演实际采用新方法的达标率待测。
- 现有4318服务未运行；下次按运行指南正常启动会加载新后台工具。未启动真实项目，不把页面静态变化写成新后端已在线运行。
- 原电影人评、21个场景族、真实参考收益和完整制作仍保留待验证或原失败状态。
