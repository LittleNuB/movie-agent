# 首轮人工反馈与资产核对

2026-09-07，用户在观看本轮作品及阅读剧本后反馈：

> 视觉效果蛮好的，但是我看不太懂讲了什么故事。

> 首先我要反馈这个剧本，感觉非常的无聊，也没有体现什么价值，甚至我都看不懂。

据此登记：本轮已展示故事的吸引力、价值表达和可理解性未通过用户评价。此前的画面观感正面反馈不代表全部镜头质量、连续性或完整产品验收通过。上下文指向《回声频率》，但用户未指定所看片段或成片版本的精确身份，因此不把意见自动归到某个文件版本，也不推定其评价了其他项目。

用户要求查看本轮所有生成资产，逐项人工核对质量。当前工作先提供完整资产入口，不据此自动改写剧本、补拍或批准共创项目。

## 审阅入口

在仓库根目录运行：

```powershell
.venv\Scripts\python.exe scripts\review_assets.py
```

打开 <http://127.0.0.1:4319/>。此入口是本轮本地验收辅助页，不替代正式产品作品页。只读读取本仓库 `local-data` 中的产物记录和媒体文件，不调用模型、不修改项目采用版本。

- 按项目、来源、媒体类型及文字搜索查看。包含原始生成、未采用候选、成片旧版本、配音、混音、母版、字幕与抽帧。
- 真实制作、真实接口验证、LLM 语义测试／受控素材、派生／未登记文件及验证截图分别标记。不能把测试素材当作真实生成质量证据。
- 保留失败／未知任务清单；没有文件的任务不能伪装成可看产物。
- 对每项填写状态、意见与时间点，意见保存在当前浏览器中；导出审核 JSON 后可交回继续处理。不会自动发送导演或执行修改。
- 清单写入被 Git 忽略的 `local-data/asset-review/manifest.json`，源文件不复制或覆盖；原始媒体规格不改动。

先前验证报告中的“待人评”是当时状态。自本记录起，故事层面已有明确负面评价；其他媒体维度仍待逐项核对，不能继续概括为“全部无人评”。

## 本次清点与入口检查

本次快照共 320 条资产／文字记录，对应 235 个不同的本地文件，83 条无文件的文字产物。文件记录有少量跨验证项目复用，不等于 235 次 API 生成；抽帧、剪辑中间文件、字幕及测试文件均在范围内。所有登记了路径的产物文件均存在。未成功任务另外列示，不计为成功产物。

浏览器实际检查了筛选、最终影片加载和跳转到 85 秒、审核意见刷新后保留、导出 JSON，以及视频／图片／声音文件的 Range 读取。页面无脚本错误，未知文件身份返回 404，非本地主机请求返回 403。自动化使用独立浏览器上下文，测试意见未写入用户项目，也不属于人工质量结论。
